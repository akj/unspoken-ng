addon/doc/*.css
addon/doc/en/
*_docHandler.py
*.html
# The smoke checklist's test page is authored, not generated: the *.html rule
# above exists for scons's readme.html output, and would otherwise swallow it.
!/docs/smoke-test-page.html
*.ini
!/pytest.ini
*.mo
*.pot
*.pyc
*.nvda-addon
.*.zip
*.zip
.sconsign.dblite
.vscode/

# Agent tooling and process docs — kept local, not shipped with the addon
.agents/
.claude/
docs/agents/
skills-lock.json

# Only the root CLAUDE.md is tracked, which points contributors at AGENTS.md.
# Per-directory CLAUDE.md files are not wanted; this keeps any that agent
# tooling regenerates from being committed.
CLAUDE.md
!/CLAUDE.md

# Scratch working area (research notes, spikes, measurement runs)
.scratch/

# Build output from Rust spikes
target/
